# 烧写好系统后对开发板的额外配置

1. 参考`rk3566核心板+底板usb配置.md`配置usb相关.
2. 更新板子内部的rknn库, [rknn-toolkit2](https://github.com/airockchip/rknn-toolkit2.git).
3. 部署rga库方便图像转换, [rk_rga](https://github.com/airockchip/librga.git)
4. 配置smb
    ```bash
    sudo apt update
    sudo apt install samba samba-vfs-modules -y
    
    sudo vim /etc/samba/smb.conf
    [global]
       workgroup = WORKGROUP
       security = user
       map to guest = never
       unix extensions = no
       follow symlinks = yes
       wide links = yes
    
    [share]
       path = /home/radxa
       force user = radxa
       browseable = yes
       writable = yes
       create mask = 0644
       directory mask = 0755
       follow symlinks = yes
       wide links = yes
    # 保存退出
    sudo smbpasswd -a radxa
    sudo systemctl restart smbd nmbd
    sudo systemctl enable smbd nmbd
    ```